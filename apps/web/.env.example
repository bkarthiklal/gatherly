# Production only — in development Vite proxies /api and /socket.io to localhost:4000.
# Netlify build environment:
API_ORIGIN=https://your-api.onrender.com
VITE_SOCKET_URL=https://your-api.onrender.com
