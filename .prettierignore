# Generated by pnpm — reformatting it produces a huge spurious diff and can
# break `pnpm install --frozen-lockfile` in CI.
pnpm-lock.yaml

# Build output and caches
dist
build
coverage
node_modules
*.tsbuildinfo

# Editor/agent tooling
.claude/
